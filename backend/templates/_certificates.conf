{% if certificate and certificate_id > 0 -%}
{% if certificate.provider == "letsencrypt" %}
  # Let's Encrypt SSL
  include conf.d/include/letsencrypt-acme-challenge.conf;
  include conf.d/include/ssl-cache.conf;
  include conf.d/include/ssl-ciphers.conf;
  ssl_certificate /home/container/etc/letsencrypt/live/npm-{{ certificate_id }}/fullchain.pem;
  ssl_certificate_key /home/container/etc/letsencrypt/live/npm-{{ certificate_id }}/privkey.pem;
{% else %}
  # Custom SSL
  ssl_certificate /home/container/data/custom_ssl/npm-{{ certificate_id }}/fullchain.pem;
  ssl_certificate_key /home/container/data/custom_ssl/npm-{{ certificate_id }}/privkey.pem;
{% endif %}
{% endif %}

