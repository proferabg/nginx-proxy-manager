{% include "_header_comment.conf" %}

server {
  listen {{ http_port }};
{% if ipv6 -%}
  listen [::]:{{ http_port }};
{% endif %}

  server_name {{ domain_names | join: " " }};

  access_log /home/container/data/logs/letsencrypt-requests_access.log standard;
  error_log /home/container/data/logs/letsencrypt-requests_error.log warn;

  include conf.d/include/letsencrypt-acme-challenge.conf;

  location / {
    return 404;
  }
}
