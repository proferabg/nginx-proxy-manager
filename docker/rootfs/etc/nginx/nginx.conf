# run nginx in foreground
daemon off;
pid /home/container/run/nginx/nginx.pid;

# Set number of worker processes automatically based on number of CPU cores.
worker_processes auto;

# Enables the use of JIT for regular expressions to speed-up their processing.
pcre_jit on;

error_log /home/container/data/logs/fallback_error.log warn;

# Includes files with directives to load dynamic modules.
include /home/container/etc/nginx/modules/*.conf;

# Custom
include /home/container/data/nginx/custom/root_top[.]conf;

events {
	include /home/container/data/nginx/custom/events[.]conf;
}

http {
	include                       /home/container/etc/nginx/mime.types;
	default_type                  application/octet-stream;
	sendfile                      on;
	server_tokens                 off;
	tcp_nopush                    on;
	tcp_nodelay                   on;
	client_body_temp_path         /home/container/.tmp/nginx/body 1 2;
	keepalive_timeout             90s;
	proxy_connect_timeout         90s;
	proxy_send_timeout            90s;
	proxy_read_timeout            90s;
	ssl_prefer_server_ciphers     on;
	gzip                          on;
	proxy_ignore_client_abort     off;
	client_max_body_size          2000m;
	server_names_hash_bucket_size 1024;
	proxy_http_version            1.1;
	proxy_set_header              X-Forwarded-Scheme $scheme;
	proxy_set_header              X-Forwarded-For $proxy_add_x_forwarded_for;
	proxy_set_header              Accept-Encoding "";
	proxy_cache                   off;
	proxy_cache_path              /home/container/var/lib/nginx/cache/public  levels=1:2 keys_zone=public-cache:30m max_size=192m;
	proxy_cache_path              /home/container/var/lib/nginx/cache/private levels=1:2 keys_zone=private-cache:5m max_size=1024m;
	proxy_temp_path               /home/container/.tmp/nginx/proxy;
	fastcgi_temp_path             /home/container/.tmp/nginx/fastcgi;
	uwsgi_temp_path               /home/container/.tmp/nginx/uwsgi;
	scgi_temp_path                /home/container/.tmp/nginx/scgi;

	# Log format and fallback log file
	include /home/container/etc/nginx/conf.d/include/log.conf;

	# Dynamically generated resolvers file
	include /home/container/etc/nginx/conf.d/include/resolvers.conf;

	# Default upstream scheme
	map $host $forward_scheme {
		default http;
	}

	# Real IP Determination

	# Local subnets:
	set_real_ip_from 10.0.0.0/8;
	set_real_ip_from 172.16.0.0/12; # Includes Docker subnet
	set_real_ip_from 192.168.0.0/16;
	# NPM generated CDN ip ranges:
	include conf.d/include/ip_ranges.conf;
	# always put the following 2 lines after ip subnets:
	real_ip_header X-Real-IP;
	real_ip_recursive on;

	# Custom
	include /home/container/data/nginx/custom/http_top[.]conf;

	# Files generated by NPM
	include /home/container/etc/nginx/conf.d/*.conf;
	include /home/container/data/nginx/default_host/*.conf;
	include /home/container/data/nginx/proxy_host/*.conf;
	include /home/container/data/nginx/redirection_host/*.conf;
	include /home/container/data/nginx/dead_host/*.conf;
	include /home/container/data/nginx/temp/*.conf;

	# Custom
	include /home/container/data/nginx/custom/http[.]conf;
}

stream {
	# Files generated by NPM
	include /home/container/data/nginx/stream/*.conf;

	# Custom
	include /home/container/data/nginx/custom/stream[.]conf;
}

# Custom
include /home/container/data/nginx/custom/root[.]conf;
